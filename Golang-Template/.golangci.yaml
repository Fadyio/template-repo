run:
  timeout: 5m
  tests: true

linters-settings:
  govet:
    enable-all: true

linters:
  disable-all: true
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
